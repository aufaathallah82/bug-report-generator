{
  "name": "bug-report-generator-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Plain JavaScript Chrome extension for recording bug reproduction evidence.",
  "scripts": {
    "build": "node scripts/validate-extension.js"
  }
}
